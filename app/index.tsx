import { View, Text } from 'react-native';

export default function Index() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-2xl font-bold text-slate-900">Squire</Text>
      <Text className="mt-2 text-sm text-slate-600">Phase 1 foundation — routing and styling alive.</Text>
    </View>
  );
}
