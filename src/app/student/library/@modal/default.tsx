// Default content for the @modal parallel slot when no modal route is active.
// Required by Next.js parallel routes so the slot resolves to nothing on
// initial navigation to /student/library.

export default function Default() {
  return null;
}
