// Default content for the @modal parallel slot when no reader route is
// active. Required by Next.js parallel routes so the slot resolves to
// nothing on initial navigation to /student/cards.

export default function Default() {
  return null;
}
